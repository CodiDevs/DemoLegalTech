import { Component } from '@angular/core';

@Component({
  selector: 'app-mock-badge',
  standalone: true,
  template: `<span class="badge-demo">Fase 2 · mock</span>`,
})
export class MockBadgeComponent {}
